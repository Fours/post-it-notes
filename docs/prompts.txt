1.

/init

2.

Create a Post-It note web app. Add a header to the top of the page. The header should contain the title "My Post-it Notes" on the left and a round button with a "plus" icon on the right.

The button in the header should create a new empty square note (300px by 300px) in the middle of the page. The note should have a small folded corner in the top left. The background color of the note should be light yellow.

The user should be able to click inside a note and then add text to it (multiple lines) and save changes. That text should then appear in the note.

Store all notes data in local storage. 

